[general]
name = "relu-cuda-oxide"
version = 1
edition = 5
license = "Apache-2.0"
backends = ["cuda"]

[general.hub]
repo-id = "kernels-test/relu-cuda-oxide"

[tvm-ffi]

[kernel.relu_cuda_oxide]
backend = "cuda"
dsl = "cuda-oxide"
depends = []
src = ["Cargo.toml", "Cargo.lock", "rust-toolchain.toml", "src", "kernels/Cargo.toml", "kernels/src"]
device-manifest = "kernels/Cargo.toml"
cuda-capabilities = ["8.0"]
