[general]
name = "relu-rust"
version = 1
edition = 5
license = "Apache-2.0"
backends = ["cpu"]

[general.hub]
repo-id = "kernels-test/relu-rust"

[tvm-ffi]

[kernel.relu_rust]
backend = "rust-cpu"
src = ["relu-rs/Cargo.toml", "relu-rs/src", "Cargo.toml", "Cargo.lock"]
